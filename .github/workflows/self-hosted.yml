name: self-hosted-runner-pipeline

on:
    push

jobs:
    self-hosted-runner-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v2
            - name: Hello world
              run: echo "This is a self-hosted runner pipeline"