name: conditional-pipeline

on:
    push

jobs:
    hello-world:
        if: github.repository == 'Twinkalkumar/GH-200-learn'
        runs-on: ubuntu-latest
        steps:
            - name: "Hello World"
              run: echo "Hello World!"
    goodbye-world:
        runs-on: ubuntu-latest
        steps:
            - name: "Goodbye World"
              run: echo "Goodbye World!"